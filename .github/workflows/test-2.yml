name: Test 2

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string
    secrets:
      token:
        required: true

jobs:
  example_job_2:
    name: Pass input and secrets to my-action
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.username }} ${{ secrets.token }}
  other_job_2:
    name: Other job 2
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.username }} ${{ secrets.token }}
  
